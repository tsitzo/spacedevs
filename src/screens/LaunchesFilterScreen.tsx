import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const LaunchesFilterScreen = () => {
  return (
    <View>
      <Text>LaunchesFilterScreen</Text>
    </View>
  );
};

export default LaunchesFilterScreen;

const styles = StyleSheet.create({});
